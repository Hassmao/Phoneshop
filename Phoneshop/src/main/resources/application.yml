spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/phoneshop
    username: phoneshop
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
